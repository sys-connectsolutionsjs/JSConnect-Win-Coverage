# Dependencias de desarrollo (incluye proxy para desarrollo completo)
# playwright llega vía requirements-proxy.txt (login asistido + tools/captura.py)
-r requirements.txt
-r requirements-proxy.txt
pytest>=8.0
ruff>=0.5
pyinstaller>=6.0